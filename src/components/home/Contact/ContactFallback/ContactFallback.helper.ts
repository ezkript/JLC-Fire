import { CONTACT_DATA } from '@/lib/constants';

export const fallbackText = {
  title: 'Contáctanos',
  message: `El formulario de contacto no está configurado. Por favor, contacta directamente a ${CONTACT_DATA.email}`
};
