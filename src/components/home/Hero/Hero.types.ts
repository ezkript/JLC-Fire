export interface HeroProps {
  // Propiedades del componente Hero
}

export interface HeroProject {
  id: string;
  title: string;
  description: string;
  image: string;
  ctaText: string;
  ctaLink: string;
}
